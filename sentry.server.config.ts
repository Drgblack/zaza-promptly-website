import * as Sentry from "@sentry/nextjs"; Sentry.init({ dsn: process.env.SENTRY_DSN, // Performance Monitoring tracesSampleRate: process.env.NODE_ENV === 'production' ? 0.1 : 1.0, // Environment setup environment: process.env.NODE_ENV, // Enhanced server-side configuration for educational app beforeSend(event, hint) { // Filter sensitive information if (event.request) { // Remove sensitive headers if (event.request.headers) { delete event.request.headers['authorization']; delete event.request.headers['cookie']; delete event.request.headers['stripe-signature']; } // Sanitize URL parameters if (event.request.url) { event.request.url = event.request.url.replace(/email=([^&]+)/g, 'email=***'); event.request.url = event.request.url.replace(/api_key=([^&]+)/g, 'api_key=***'); } // Sanitize request data if (event.request.data && typeof event.request.data === 'object') { const sanitized = { ...event.request.data } as any; if (sanitized.email) sanitized.email = '***@***.***'; if (sanitized.apiKey) sanitized.apiKey = '***'; if (sanitized.password) sanitized.password = '***'; event.request.data = sanitized; } } // Enhanced error context for educational environment if (event.exception) { const error = hint.originalException; // Don't send certain types of errors if (error instanceof Error) { // Skip common development/network errors if (error.message.includes('ENOTFOUND') || error.message.includes('ECONNREFUSED') || error.message.includes('Network request failed')) { return null; } } } return event; }, // Custom tags for server context initialScope: { tags: { component: "server", app: "zaza-promptly", userType: "teacher" }, }, // Server-specific integrations - simplified for compatibility integrations: [],
});