# Zaza StackMate - Technical Background Zaza StackMate is built to be robust, scalable, and flexible for non-technical users. ## Core Architecture - **Frontend**: Built with React and Tailwind UI - **Backend**: Node.js + Express with scalable OpenAI integration - **Database**: Firestore for user agents, prompt storage, and permissions - **Security**: OAuth2 login, role-based access, encrypted prompt storage ## AI Engine - StackMate uses OpenAI’s GPT-4-turbo model for all generative tasks. - Dynamic prompt templates allow users to parameterize and reuse workflows. - We employ instruction tuning to align responses with business and educational needs. ## Agent Structure Each StackMate agent consists of: - System Prompt + User Prompt template - Optional context memory - Output formatting and auto-tagging - User-defined triggers or integrations access_conditions: - if: user.email == "greg@zazatechnologies.com" allow: full_access - if: user.email == "greg.blackburn@gmail.com" allow: full_access
