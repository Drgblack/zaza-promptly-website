"use client";

import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Play, Brain, Palette, Shield, FolderOpen, Share, Users, Star, Check } from "lucide-react";
import { HeroCTA } from "@/components/hero-cta";
import { EmailSignup } from "@/components/email-signup";
import { VisualGallery } from "@/components/visual-gallery";
import { Layout } from "@zaza/shared-components";
import { headingLg, headingXl } from "../../shared-components/styles/ui";

export default function ZazaVisualsLanding() {
  return (
    <Layout currentProduct="Visuals">
      {/* ...rest of the component code from the original page.tsx... */}
    </Layout>
  );
} 